import React, {FC} from 'react';
import LoginForm from "@/components/ui/LoginForm/LoginForm";

const Page:FC = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
};

export default Page;
